window.SITE_CONFIG = {
  // Change this after creating your GitHub repository.
  // Example: "https://github.com/your-name/ai-hack-watch"
  repositoryUrl: "https://github.com/agnivesh/ai-hack-watch",
  // Public URL of the deployed site (GitHub Pages). Used to fill the
  // canonical / og:url / og:image tags in index.html at build time, so
  // social crawlers get absolute URLs even without JavaScript.
  // Example: "https://your-name.github.io/ai-hack-watch"
  siteUrl: "https://agnivesh.github.io/ai-hack-watch"
};